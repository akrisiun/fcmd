<UserControl x:Class="fcmd.View.Xaml.PanelCmd"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:fcmd.View"
             xmlns:ctrl="clr-namespace:fcmd.View.ctrl"
             mc:Ignorable="d" 
             d:DesignHeight="25" d:DesignWidth="600">
    <Grid VerticalAlignment="Stretch" HorizontalAlignment="Stretch" Background="White">
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="40" />
            <ColumnDefinition Width=".4*" MinWidth="120" />
            <ColumnDefinition Width="75" />
            <ColumnDefinition Width=".4*" MinWidth="150" />
        </Grid.ColumnDefinitions>
        <Label>Item:</Label>
        <ctrl:TextEntry x:Name="selected"  x:FieldModifier="public" 
                 Margin="5 1 5 1" Padding="0 2 0 1"
                 HorizontalAlignment="Stretch" Grid.Column="1" />
        <Label Grid.Column="2"
            >Command:</Label>
        <ctrl:ComboWidget x:Name="cmd" x:FieldModifier="public"
                 Margin="5 1 24 1"  Padding="0 2 0 1"
                 IsEditable="True" 
                 HorizontalAlignment="Stretch" Grid.Column="3" />
    </Grid>
</UserControl>
